language: node_js

node_js:
    - 4.4.7

services:
    - selenium

addons:
    firefox: "17.0"

build:
    ci:
        - npm install
        - npm run devserver
        - cp index.html.sample index.html
        - export DISPLAY=:99.0
        - xvfb-run --server-args="-ac" npm run test_on_ci
