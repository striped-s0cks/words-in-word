language: node_js

node_js:
    - 4.4.7

services:
  - selenium

build:
    ci:
        - npm install
        - npm run lint
        - npm install -g selenium-standalone
        - selenium-standalone install
        - npm run test
